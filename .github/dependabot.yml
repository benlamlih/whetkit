version: 2
updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    labels: [dependencies]
    # Dependencies are pinned exactly and documented in VERSIONS.md — update
    # the table (version, check date, source) when merging these PRs.

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels: [dependencies, ci]
